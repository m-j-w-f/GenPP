# Variable configuration for full WeatherBench2 dataset
input_features: 29
aggregation_levels: 2
meta_features: 4 # The pixel index is excluded here
output: 2
input_concat: ${eval:'${.input_features} * ${.aggregation_levels} + ${.meta_features} + (${model.embedding_dim} > 0)'}
