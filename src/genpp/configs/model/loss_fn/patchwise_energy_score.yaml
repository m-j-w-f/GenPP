_target_: genpp.models.scores.PatchwiseEnergyScore
beta: 1.0
clamp: true
patch_size: 3
normalize: true
# Note: The PatchwiseEnergyScore class defaults to unbiased=True.
# We explicitly set unbiased: false here to preserve existing behavior / backward compatibility.
unbiased: false
